# آدرسِ API برای فرانت‌اند. اگر API روی همین ماشین و پورتِ ۴۰۰۰ است، نیازی به ست‌کردن
# نیست (پیش‌فرض همین است). برای میزبانِ دیگر، این را به آدرسِ واقعیِ API تغییر دهید.
NEXT_PUBLIC_API_URL="http://localhost:4000/api"
